import { OptimalBorderDirective } from './optimal-border.directive';

describe('OptimalBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new OptimalBorderDirective();
    expect(directive).toBeTruthy();
  });
});
